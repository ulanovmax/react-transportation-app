.card-type {
    img {
        opacity: 0.5;
        width: 100%;
        height: 100%;
        object-fit: cover;
        transition: 0.3s ease;
    }

    &:hover {
        img {
            opacity: 1;
            transform: scale(1.05);
        }
    }
}